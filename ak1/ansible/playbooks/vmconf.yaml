---
- name: Test
  hosts: vm2
  remote_user: adm02
  tasks:
  - name: Opprett gruppe
    become: true
    ansible.builtin.group:
      name: student
      state: present
  
  - name: Opprett bruker 1
    become: true
    ansible.builtin.user:
      name: remdro
      password: "{{ 'Passord123' | password_hash('sha512') }}"
      state: present
      groups: student
      append: yes

  - name: Opprett bruker 2
    become: true
    ansible.builtin.user:
      name: jondoe
      password: "{{ 'Passord123' | password_hash('sha512') }}"
      state: present
      groups: student
      append: yes

  - name: Installer program
    become: true
    ansible.builtin.apt:
      pkg: nmap
      state: present